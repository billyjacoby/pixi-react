export const PLAYER_SIZE = {
  width: 128,
  height: 128,
};

export const TILE_SIZE = 256;

export const PERSPECTIVE_HEIGHT_RATIO = 0.3;

// export const DEBUG = process.env.NODE_ENV === 'development';
export const DEBUG = true;
